// Navigation
export const navHoverBar = { hover: { width: "100%" } };

// Main content
export const regionFilterFade = {
  hidden: { opacity: 0 },
  visible: { opacity: 1 },
};

export const regionBox = {
  hidden: { height: 0 },
  visible: { height: "max-content", transition: { staggerChildren: 0.05 } },
};
